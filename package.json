{
  "name": "deku-test",
  "version": "0.0.0",
  "description": "deku test",
  "scripts": {
    "watch": "$(npm bin)/watchify -v -t babelify --extension='.js' -o bundle.js index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mizchi",
  "license": "MIT",
  "devDependencies": {
    "babelify": "^6.1.2",
    "browserify": "^9.0.8",
    "watchify": "^3.1.1"
  },
  "dependencies": {
    "deku": "^0.4.5"
  }
}
